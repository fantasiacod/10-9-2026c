# The site's database, its backup mirror and the storage settings live
# here. None of it may ever be downloaded over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
